# AGENTS.md

这是 Lincoln Island 仓库中所有人类与 AI 协作者的共同入口。目标是让新的协作者只依赖仓库内容，也能迅速理解项目、继续工作，并把有长期价值的新认识留给下一位协作者。

## 开始任何任务前

按顺序阅读：

1. [`docs/NOW.md`](docs/NOW.md)：当前阶段、最新进展、下一步和阻塞项。
2. [`docs/VISION.md`](docs/VISION.md)：项目为何存在、服务谁、坚持什么。
3. [`docs/MVP.md`](docs/MVP.md)：当前里程碑的范围和验收标准。
4. [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)：已知的系统边界与数据模型。
5. [`docs/decisions/README.md`](docs/decisions/README.md) 及与任务相关的 ADR：重要决定及其原因。
6. 与任务直接相关的代码、测试和参考资料。

然后检查工作区状态，保留不属于本任务的修改。不要仅凭聊天记录、模型记忆或文件名猜测项目现状。

## 项目的一句话定义

Lincoln Island 首先是一个帮助儿童阅读儒勒·凡尔纳《神秘岛》的可漫游 3D 章节地图，而不是生存游戏：读者选择某一章，便进入整座岛在该章时刻的状态。

## 不可悄悄改变的产品约束

- 3D 世界用于引导读者回到书中，不替代阅读。
- 章节是全岛的时间切片，不是彼此割裂的传统关卡。
- 永久地理、文明建设、读者在当章已知的信息必须分层处理。
- 前期优先验证空间理解与阅读体验，不扩张到战斗、经济、联网或完整生存玩法。
- 内容不得提前暴露小说尚未揭晓的地点、身份或事件。
- 小说原文、译本、插画、地图、音频和 AI 生成资产都必须分别记录来源与使用权；“原著年代久远”不代表某个译本或地图可自由使用。

若任务需要改变以上约束，应先写 ADR；无法从现有资料判断时，标记为“待确认”，不要替项目所有者作不可逆决定。

## 工程原则

- **代码与数据优先**：场景状态、地标、路线和章节变化应能从版本化的数据重建，避免只有某台机器上的引擎场景或编辑器手工状态才是事实。
- **先灰盒，后美术**：先用简单几何体验证尺度、路线和可读性，再投入生成式 3D 资产。
- **AI 资产离线生产**：早期将混元等服务用于开发期资产生产，而非发布版本中的运行时依赖。
- **可复现**：生成脚本、输入描述、随机种子、模型版本、人工修改和许可证信息应随资产登记。
- **儿童友好**：交互、文字、导航、隐私和外部链接按儿童阅读场景审视。
- **小步验证**：每次扩大岛屿或章节范围前，先证明当前纵切可走、可定位、可理解。
- **Docker 优先**：开发、测试和构建默认通过版本化的容器入口完成，宿主机只需 Docker Desktop、Git 和必要的图形工具；避免要求协作者在宿主机全局安装 Node 或写入 `node_modules`。容器应锁定基础镜像、使用非 root 用户、保留依赖锁文件，并提供健康检查与清理方法。Docker 不是目的；浏览器体验、Blender 等图形工作或明显更适合宿主机的操作可以留在宿主机，但须记录前提。

当前技术方向以 Web、Babylon.js、TypeScript 和 GLB/glTF 为基线，详见 ADR 0004。精确包版本、浏览器支持范围和性能预算仍须通过技术验证确定；不要把提案或最新版本号写成已经验证的事实。

## 仓库记忆协议

仓库中的记忆分四类：

| 记忆 | 文件 | 何时更新 |
|---|---|---|
| 长期意图 | `docs/VISION.md`、`docs/MVP.md` | 产品目标或范围真正改变时 |
| 当前状态 | `docs/NOW.md` | 完成一项实质工作、发现阻塞或改变下一步时 |
| 决策原因 | `docs/decisions/*.md` | 做出影响后续实现的选择时 |
| 实现事实 | 代码、测试、`docs/ARCHITECTURE.md` | 行为、边界或数据结构改变时 |

工作结束前：

1. 运行与变更风险相称的检查或测试。
2. 更新 `docs/NOW.md`，删除过时状态，而不是不断追加会话流水账。
3. 若产生长期决定，依据模板新增 ADR，并更新 ADR 索引。
4. 若代码与文档冲突，要么一起修正，要么把冲突明确记录为阻塞项。
5. 交接信息必须写出可执行的下一步、相关路径和验证结果。

不要把内部推理、聊天摘要、赞美性文字或很快过期的细节写入项目记忆。日期使用 `YYYY-MM-DD`；未知事实写“待确认”；临时推断写“假设”。

## 版本控制与协作

- 以可独立理解、可独立验证、必要时可安全回退的小节点提交，不把多个无关目标塞进同一提交。
- 提交前检查工作区并保留其他人或 Agent 的修改；共享仓库中发现不明改动时，先调查来源，不覆盖或删除。
- Git commit 的 subject 使用简洁中文描述成果；body 使用中文概括“完成了什么”和“如何验证”，必要时记录遗留风险。
- 只在相应检查通过后提交；测试或环境验证未执行时必须如实写明，不能用“完成”“通过”等字样掩盖。
- 不擅自 push、发布或创建 PR；这些外部动作需要项目所有者明确授权。
- 不假设其他 Codex 任务能看到当前聊天。任务交接依靠仓库 memory、实际代码、测试和明确的任务消息。

## 事实优先级

发生冲突时，按以下顺序调查并修复不一致：

1. 能运行的代码、自动化测试和实际资产数据。
2. 已接受且未被取代的 ADR。
3. `VISION.md`、`MVP.md` 和 `ARCHITECTURE.md`。
4. `NOW.md`（它是当前快照，可能最先过时）。
5. 外部聊天和个人记忆。

代码是行为事实，但不能自行推翻产品意图；发现两者冲突时应显式报告。
